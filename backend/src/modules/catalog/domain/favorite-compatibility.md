# Favorite compatibility

Future favorites must reference only `userId` and `masterServiceId`. Provider offerings are routing infrastructure and must not be part of a favorite identity. Provider replacement or disappearance therefore preserves the MasterService reference and does not require a Favorites implementation in this feature.
